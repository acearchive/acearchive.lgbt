{{- range $page := .Site.Pages -}}
{{- range $alias := $page.Aliases -}}
{{ if eq $page.Type "archive" }}{{ path.Join "artifact" $alias | relURL }}{{ else }}{{ $alias }}{{ end }} {{ $page.RelPermalink }}
{{ end -}}
{{- end -}}
